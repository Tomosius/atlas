{
  "name": "sample-typescript-project",
  "version": "0.1.0",
  "description": "Sample TypeScript project fixture for Atlas tests",
  "scripts": {
    "build": "tsc",
    "lint": "eslint src",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.0.0",
    "eslint": "^9.0.0",
    "vitest": "^2.0.0"
  }
}
